# Python dependencies for AlphaSense Dashboard
pyserial>=3.5
bleak>=0.21.1        # BLE (cross-platform)
pystray>=0.19.5      # System tray icon
Pillow>=10.0.0       # Required by pystray for icon rendering
